const indexRouter = require('./homeRoutes');
const testRouter = require('./questRoutes');
const dotoryRouter = require('./dotoryRoutes');

module.exports = {
    indexRouter,
    testRouter,
    dotoryRouter
};